deploy-pages:
  stage: deploy
  script:
    - mkdir -p public
    - test -f public/index.html || echo '<!doctype html><html><body>Zaya assets online</body></html>' > public/index.html
  pages:
    publish: public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH