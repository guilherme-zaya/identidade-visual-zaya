deploy-pages:
  stage: deploy
  script:
    - test -f public/index.html || echo '<!doctype html><html><body>OK</body></html>' > public/index.html
  pages:
    publish: public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH